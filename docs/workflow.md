# Development Workflow

1. Collect gestures using `src/dataset_collection/collect_data.py`.
2. Preprocess and augment data (`src/preprocessing`).
3. Train models: `python -m src.training.train`.
4. Run `main.py` for real-time demo.
